// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a data source to read a VMware Cloud Director Site association data to be used for association with another site.
 *
 * Supported in provider *v3.13+*
 *
 * ## Example Usage
 *
 * Note: there is only one site available for each VCD. No ID or name is necessary to identify it.
 *
 * > Note: this data source requires System Administrator privileges
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const currentSite = vcd.getMultisiteSiteData({
 *     downloadToFile: "filename.xml",
 * });
 * ```
 */
export function getMultisiteSiteData(args?: GetMultisiteSiteDataArgs, opts?: pulumi.InvokeOptions): Promise<GetMultisiteSiteDataResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vcd:index/getMultisiteSiteData:getMultisiteSiteData", {
        "downloadToFile": args.downloadToFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getMultisiteSiteData.
 */
export interface GetMultisiteSiteDataArgs {
    /**
     * Name of the file that will contain the data needed to associate this site to a remote one.
     * Contains the same data returned in `associationData`.
     */
    downloadToFile?: string;
}

/**
 * A collection of values returned by getMultisiteSiteData.
 */
export interface GetMultisiteSiteDataResult {
    /**
     * The data needed to associate this site to another one. Contains the same data that would be saved into
     * the file defined in `downloadToFile`.
     */
    readonly associationData: string;
    readonly downloadToFile?: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
}
/**
 * Provides a data source to read a VMware Cloud Director Site association data to be used for association with another site.
 *
 * Supported in provider *v3.13+*
 *
 * ## Example Usage
 *
 * Note: there is only one site available for each VCD. No ID or name is necessary to identify it.
 *
 * > Note: this data source requires System Administrator privileges
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const currentSite = vcd.getMultisiteSiteData({
 *     downloadToFile: "filename.xml",
 * });
 * ```
 */
export function getMultisiteSiteDataOutput(args?: GetMultisiteSiteDataOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetMultisiteSiteDataResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vcd:index/getMultisiteSiteData:getMultisiteSiteData", {
        "downloadToFile": args.downloadToFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getMultisiteSiteData.
 */
export interface GetMultisiteSiteDataOutputArgs {
    /**
     * Name of the file that will contain the data needed to associate this site to a remote one.
     * Contains the same data returned in `associationData`.
     */
    downloadToFile?: pulumi.Input<string>;
}
