// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides the capability of fetching an existing RDE Interface Behavior from VMware Cloud Director.
 *
 * Supported in provider *v3.10+*. Requires System Administrator privileges.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const myInterface = vcd.getRdeInterface({
 *     vendor: "vmware",
 *     nss: "k8s",
 *     version: "1.0.0",
 * });
 * const myBehavior = myInterface.then(myInterface => vcd.getRdeInterfaceBehavior({
 *     rdeInterfaceId: myInterface.id,
 *     name: "createKubeConfig",
 * }));
 * export const executionId = myBehavior.then(myBehavior => myBehavior.execution?.id);
 * export const executionType = myBehavior.then(myBehavior => myBehavior.execution?.type);
 * ```
 */
export function getRdeInterfaceBehavior(args: GetRdeInterfaceBehaviorArgs, opts?: pulumi.InvokeOptions): Promise<GetRdeInterfaceBehaviorResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vcd:index/getRdeInterfaceBehavior:getRdeInterfaceBehavior", {
        "name": args.name,
        "rdeInterfaceId": args.rdeInterfaceId,
    }, opts);
}

/**
 * A collection of arguments for invoking getRdeInterfaceBehavior.
 */
export interface GetRdeInterfaceBehaviorArgs {
    /**
     * The name of the RDE Interface Behavior to fetch
     */
    name: string;
    /**
     * The ID of the RDE Interface that owns the Behavior to fetch
     */
    rdeInterfaceId: string;
}

/**
 * A collection of values returned by getRdeInterfaceBehavior.
 */
export interface GetRdeInterfaceBehaviorResult {
    readonly description: string;
    /**
     * @deprecated This argument cannot consider complex execution structures. For that purpose, use 'execution_json' instead
     */
    readonly execution: {[key: string]: string};
    readonly executionJson: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly name: string;
    readonly rdeInterfaceId: string;
    readonly ref: string;
}
/**
 * Provides the capability of fetching an existing RDE Interface Behavior from VMware Cloud Director.
 *
 * Supported in provider *v3.10+*. Requires System Administrator privileges.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const myInterface = vcd.getRdeInterface({
 *     vendor: "vmware",
 *     nss: "k8s",
 *     version: "1.0.0",
 * });
 * const myBehavior = myInterface.then(myInterface => vcd.getRdeInterfaceBehavior({
 *     rdeInterfaceId: myInterface.id,
 *     name: "createKubeConfig",
 * }));
 * export const executionId = myBehavior.then(myBehavior => myBehavior.execution?.id);
 * export const executionType = myBehavior.then(myBehavior => myBehavior.execution?.type);
 * ```
 */
export function getRdeInterfaceBehaviorOutput(args: GetRdeInterfaceBehaviorOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetRdeInterfaceBehaviorResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vcd:index/getRdeInterfaceBehavior:getRdeInterfaceBehavior", {
        "name": args.name,
        "rdeInterfaceId": args.rdeInterfaceId,
    }, opts);
}

/**
 * A collection of arguments for invoking getRdeInterfaceBehavior.
 */
export interface GetRdeInterfaceBehaviorOutputArgs {
    /**
     * The name of the RDE Interface Behavior to fetch
     */
    name: pulumi.Input<string>;
    /**
     * The ID of the RDE Interface that owns the Behavior to fetch
     */
    rdeInterfaceId: pulumi.Input<string>;
}
