// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Supported in provider *v3.4+* and VCD 10.2+ with NSX-T and ALB.
 *
 * Provides a data source to manage ALB Clouds for Providers. An NSX-T Cloud is a service provider-level construct that
 * consists of an NSX-T Manager and an NSX-T Data Center transport zone.
 *
 * > Only `System Administrator` can use this data source.
 *
 * > VCD 10.3.0 has a caching bug which prevents listing importable clouds immediately (retrieved using
 * [`vcd.getNsxtAlbImportableCloud`](https://www.terraform.io/providers/vmware/vcd/latest/docs/data-sources/nsxt_alb_importable_cloud)) after ALB
 * Controller is created. This data should be available 15 minutes after the Controller is created.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const first = vcd.getNsxtAlbCloud({
 *     name: "cloud-one",
 * });
 * ```
 */
export function getNsxtAlbCloud(args: GetNsxtAlbCloudArgs, opts?: pulumi.InvokeOptions): Promise<GetNsxtAlbCloudResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vcd:index/getNsxtAlbCloud:getNsxtAlbCloud", {
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getNsxtAlbCloud.
 */
export interface GetNsxtAlbCloudArgs {
    /**
     * Name of ALB Cloud
     */
    name: string;
}

/**
 * A collection of values returned by getNsxtAlbCloud.
 */
export interface GetNsxtAlbCloudResult {
    readonly controllerId: string;
    readonly description: string;
    readonly healthMessage: string;
    readonly healthStatus: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly importableCloudId: string;
    readonly name: string;
    readonly networkPoolId: string;
    readonly networkPoolName: string;
}
/**
 * Supported in provider *v3.4+* and VCD 10.2+ with NSX-T and ALB.
 *
 * Provides a data source to manage ALB Clouds for Providers. An NSX-T Cloud is a service provider-level construct that
 * consists of an NSX-T Manager and an NSX-T Data Center transport zone.
 *
 * > Only `System Administrator` can use this data source.
 *
 * > VCD 10.3.0 has a caching bug which prevents listing importable clouds immediately (retrieved using
 * [`vcd.getNsxtAlbImportableCloud`](https://www.terraform.io/providers/vmware/vcd/latest/docs/data-sources/nsxt_alb_importable_cloud)) after ALB
 * Controller is created. This data should be available 15 minutes after the Controller is created.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const first = vcd.getNsxtAlbCloud({
 *     name: "cloud-one",
 * });
 * ```
 */
export function getNsxtAlbCloudOutput(args: GetNsxtAlbCloudOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetNsxtAlbCloudResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vcd:index/getNsxtAlbCloud:getNsxtAlbCloud", {
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getNsxtAlbCloud.
 */
export interface GetNsxtAlbCloudOutputArgs {
    /**
     * Name of ALB Cloud
     */
    name: pulumi.Input<string>;
}
