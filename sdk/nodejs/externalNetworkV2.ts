// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as enums from "./types/enums";
import * as utilities from "./utilities";

export class ExternalNetworkV2 extends pulumi.CustomResource {
    /**
     * Get an existing ExternalNetworkV2 resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ExternalNetworkV2State, opts?: pulumi.CustomResourceOptions): ExternalNetworkV2 {
        return new ExternalNetworkV2(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vcd:index/externalNetworkV2:ExternalNetworkV2';

    /**
     * Returns true if the given object is an instance of ExternalNetworkV2.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ExternalNetworkV2 {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ExternalNetworkV2.__pulumiType;
    }

    /**
     * An Org ID that this network should be
     * dedicated to. Only applicable when `use_ip_spaces=true`
     */
    public readonly dedicatedOrgId!: pulumi.Output<string>;
    /**
     * Network friendly description
     */
    public readonly description!: pulumi.Output<string | undefined>;
    /**
     * One or more IP scopes for the network. See IP Scope below for details.
     */
    public readonly ipScopes!: pulumi.Output<outputs.ExternalNetworkV2IpScope[] | undefined>;
    /**
     * A unique name for the network
     */
    public readonly name!: pulumi.Output<string>;
    /**
     * Configure intentions for
     * NAT and Firewall rule configuration:
     * * `EDGE_GATEWAY` - Allow management of NAT and firewall rules only on Edge Gateways. This is the
     * default behavior.
     * * `PROVIDER_GATEWAY` - Allow management of NAT and firewall rules only on Provider Gateways.
     * * `PROVIDER_AND_EDGE_GATEWAY` - Allow management of NAT and firewall rules on both the Provider and
     * Edge gateways.
     */
    public readonly natAndFirewallServiceIntention!: pulumi.Output<string>;
    /**
     * NSX-T network definition. See NSX-T Network below for details.
     */
    public readonly nsxtNetwork!: pulumi.Output<outputs.ExternalNetworkV2NsxtNetwork | undefined>;
    /**
     * Configure intentions for
     * Org VDC network Route Advertisement:
     * * `IP_SPACE_UPLINKS_ADVERTISED_STRICT` - All networks within IP Space associated with IP Space
     * Uplink will be advertised by default. This can be changed on an individual network level later,
     * if necessary. All other networks outside of IP Spaces associated with IP Space Uplinks cannot be
     * configured to be advertised. This is the default behavior.
     * * `IP_SPACE_UPLINKS_ADVERTISED_FLEXIBLE` - All networks within IP Space associated with IP Space
     * Uplink will be advertised by default. This can be changed on an individual network level later,
     * if necessary. All other networks outside of IP Spaces associated with IP Space Uplinks are not
     * advertised by default but can be configured to be advertised after creation.
     * * `ALL_NETWORKS_ADVERTISED` - All networks, regardless on whether they fall inside of any IP Spaces
     * associated with IP Space Uplinks, will be advertised by default. This can be changed on an
     * individual network level later, if necessary.
     *
     * <a id="ipscope"></a>
     */
    public readonly routeAdvertisementIntention!: pulumi.Output<string>;
    /**
     * Defines if the network uses IP Spaces. Do
     * not specify `ipScope` when using IP Spaces. (default `false`)
     */
    public readonly useIpSpaces!: pulumi.Output<boolean | undefined>;
    /**
     * One or more blocks of vSphere Network..
     */
    public readonly vsphereNetworks!: pulumi.Output<outputs.ExternalNetworkV2VsphereNetwork[] | undefined>;

    /**
     * Create a ExternalNetworkV2 resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ExternalNetworkV2Args, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ExternalNetworkV2Args | ExternalNetworkV2State, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ExternalNetworkV2State | undefined;
            resourceInputs["dedicatedOrgId"] = state ? state.dedicatedOrgId : undefined;
            resourceInputs["description"] = state ? state.description : undefined;
            resourceInputs["ipScopes"] = state ? state.ipScopes : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["natAndFirewallServiceIntention"] = state ? state.natAndFirewallServiceIntention : undefined;
            resourceInputs["nsxtNetwork"] = state ? state.nsxtNetwork : undefined;
            resourceInputs["routeAdvertisementIntention"] = state ? state.routeAdvertisementIntention : undefined;
            resourceInputs["useIpSpaces"] = state ? state.useIpSpaces : undefined;
            resourceInputs["vsphereNetworks"] = state ? state.vsphereNetworks : undefined;
        } else {
            const args = argsOrState as ExternalNetworkV2Args | undefined;
            resourceInputs["dedicatedOrgId"] = args ? args.dedicatedOrgId : undefined;
            resourceInputs["description"] = args ? args.description : undefined;
            resourceInputs["ipScopes"] = args ? args.ipScopes : undefined;
            resourceInputs["name"] = args ? args.name : undefined;
            resourceInputs["natAndFirewallServiceIntention"] = args ? args.natAndFirewallServiceIntention : undefined;
            resourceInputs["nsxtNetwork"] = args ? args.nsxtNetwork : undefined;
            resourceInputs["routeAdvertisementIntention"] = args ? args.routeAdvertisementIntention : undefined;
            resourceInputs["useIpSpaces"] = args ? args.useIpSpaces : undefined;
            resourceInputs["vsphereNetworks"] = args ? args.vsphereNetworks : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ExternalNetworkV2.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ExternalNetworkV2 resources.
 */
export interface ExternalNetworkV2State {
    /**
     * An Org ID that this network should be
     * dedicated to. Only applicable when `use_ip_spaces=true`
     */
    dedicatedOrgId?: pulumi.Input<string>;
    /**
     * Network friendly description
     */
    description?: pulumi.Input<string>;
    /**
     * One or more IP scopes for the network. See IP Scope below for details.
     */
    ipScopes?: pulumi.Input<pulumi.Input<inputs.ExternalNetworkV2IpScope>[]>;
    /**
     * A unique name for the network
     */
    name?: pulumi.Input<string>;
    /**
     * Configure intentions for
     * NAT and Firewall rule configuration:
     * * `EDGE_GATEWAY` - Allow management of NAT and firewall rules only on Edge Gateways. This is the
     * default behavior.
     * * `PROVIDER_GATEWAY` - Allow management of NAT and firewall rules only on Provider Gateways.
     * * `PROVIDER_AND_EDGE_GATEWAY` - Allow management of NAT and firewall rules on both the Provider and
     * Edge gateways.
     */
    natAndFirewallServiceIntention?: pulumi.Input<string>;
    /**
     * NSX-T network definition. See NSX-T Network below for details.
     */
    nsxtNetwork?: pulumi.Input<inputs.ExternalNetworkV2NsxtNetwork>;
    /**
     * Configure intentions for
     * Org VDC network Route Advertisement:
     * * `IP_SPACE_UPLINKS_ADVERTISED_STRICT` - All networks within IP Space associated with IP Space
     * Uplink will be advertised by default. This can be changed on an individual network level later,
     * if necessary. All other networks outside of IP Spaces associated with IP Space Uplinks cannot be
     * configured to be advertised. This is the default behavior.
     * * `IP_SPACE_UPLINKS_ADVERTISED_FLEXIBLE` - All networks within IP Space associated with IP Space
     * Uplink will be advertised by default. This can be changed on an individual network level later,
     * if necessary. All other networks outside of IP Spaces associated with IP Space Uplinks are not
     * advertised by default but can be configured to be advertised after creation.
     * * `ALL_NETWORKS_ADVERTISED` - All networks, regardless on whether they fall inside of any IP Spaces
     * associated with IP Space Uplinks, will be advertised by default. This can be changed on an
     * individual network level later, if necessary.
     *
     * <a id="ipscope"></a>
     */
    routeAdvertisementIntention?: pulumi.Input<string>;
    /**
     * Defines if the network uses IP Spaces. Do
     * not specify `ipScope` when using IP Spaces. (default `false`)
     */
    useIpSpaces?: pulumi.Input<boolean>;
    /**
     * One or more blocks of vSphere Network..
     */
    vsphereNetworks?: pulumi.Input<pulumi.Input<inputs.ExternalNetworkV2VsphereNetwork>[]>;
}

/**
 * The set of arguments for constructing a ExternalNetworkV2 resource.
 */
export interface ExternalNetworkV2Args {
    /**
     * An Org ID that this network should be
     * dedicated to. Only applicable when `use_ip_spaces=true`
     */
    dedicatedOrgId?: pulumi.Input<string>;
    /**
     * Network friendly description
     */
    description?: pulumi.Input<string>;
    /**
     * One or more IP scopes for the network. See IP Scope below for details.
     */
    ipScopes?: pulumi.Input<pulumi.Input<inputs.ExternalNetworkV2IpScope>[]>;
    /**
     * A unique name for the network
     */
    name?: pulumi.Input<string>;
    /**
     * Configure intentions for
     * NAT and Firewall rule configuration:
     * * `EDGE_GATEWAY` - Allow management of NAT and firewall rules only on Edge Gateways. This is the
     * default behavior.
     * * `PROVIDER_GATEWAY` - Allow management of NAT and firewall rules only on Provider Gateways.
     * * `PROVIDER_AND_EDGE_GATEWAY` - Allow management of NAT and firewall rules on both the Provider and
     * Edge gateways.
     */
    natAndFirewallServiceIntention?: pulumi.Input<string>;
    /**
     * NSX-T network definition. See NSX-T Network below for details.
     */
    nsxtNetwork?: pulumi.Input<inputs.ExternalNetworkV2NsxtNetwork>;
    /**
     * Configure intentions for
     * Org VDC network Route Advertisement:
     * * `IP_SPACE_UPLINKS_ADVERTISED_STRICT` - All networks within IP Space associated with IP Space
     * Uplink will be advertised by default. This can be changed on an individual network level later,
     * if necessary. All other networks outside of IP Spaces associated with IP Space Uplinks cannot be
     * configured to be advertised. This is the default behavior.
     * * `IP_SPACE_UPLINKS_ADVERTISED_FLEXIBLE` - All networks within IP Space associated with IP Space
     * Uplink will be advertised by default. This can be changed on an individual network level later,
     * if necessary. All other networks outside of IP Spaces associated with IP Space Uplinks are not
     * advertised by default but can be configured to be advertised after creation.
     * * `ALL_NETWORKS_ADVERTISED` - All networks, regardless on whether they fall inside of any IP Spaces
     * associated with IP Space Uplinks, will be advertised by default. This can be changed on an
     * individual network level later, if necessary.
     *
     * <a id="ipscope"></a>
     */
    routeAdvertisementIntention?: pulumi.Input<string>;
    /**
     * Defines if the network uses IP Spaces. Do
     * not specify `ipScope` when using IP Spaces. (default `false`)
     */
    useIpSpaces?: pulumi.Input<boolean>;
    /**
     * One or more blocks of vSphere Network..
     */
    vsphereNetworks?: pulumi.Input<pulumi.Input<inputs.ExternalNetworkV2VsphereNetwork>[]>;
}
