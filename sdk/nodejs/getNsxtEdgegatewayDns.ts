// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as enums from "./types/enums";
import * as utilities from "./utilities";

/**
 * Supported in provider *v3.11+* and VCD *10.4+* with NSX-T.
 *
 * Provides a data source to read NSX-T Edge Gateway DNS forwarder configuration.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const existing = vcd.getOrgVdc({
 *     name: "existing-vdc",
 * });
 * const testing = existing.then(existing => vcd.getNsxtEdgegateway({
 *     ownerId: existing.id,
 *     name: "server-testing",
 * }));
 * const dns_service = testing.then(testing => vcd.getNsxtEdgegatewayDns({
 *     org: "datacloud",
 *     edgeGatewayId: testing.id,
 * }));
 * ```
 */
export function getNsxtEdgegatewayDns(args: GetNsxtEdgegatewayDnsArgs, opts?: pulumi.InvokeOptions): Promise<GetNsxtEdgegatewayDnsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vcd:index/getNsxtEdgegatewayDns:getNsxtEdgegatewayDns", {
        "edgeGatewayId": args.edgeGatewayId,
        "org": args.org,
    }, opts);
}

/**
 * A collection of arguments for invoking getNsxtEdgegatewayDns.
 */
export interface GetNsxtEdgegatewayDnsArgs {
    /**
     * The ID of the Edge Gateway (NSX-T only). 
     * Can be looked up using [`vcd.NsxtEdgegateway`](https://www.terraform.io/providers/vmware/vcd/latest/docs/data-sources/nsxt_edgegateway) data source
     */
    edgeGatewayId: string;
    /**
     * The name of organization to use, optional if defined at 
     * provider level. Useful when connected as sysadmin working across different organisations
     */
    org?: string;
}

/**
 * A collection of values returned by getNsxtEdgegatewayDns.
 */
export interface GetNsxtEdgegatewayDnsResult {
    readonly conditionalForwarderZones: outputs.GetNsxtEdgegatewayDnsConditionalForwarderZone[];
    readonly defaultForwarderZones: outputs.GetNsxtEdgegatewayDnsDefaultForwarderZone[];
    readonly edgeGatewayId: string;
    readonly enabled: boolean;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly listenerIp: string;
    readonly org?: string;
    readonly snatRuleEnabled: boolean;
    readonly snatRuleIpAddress: string;
}
/**
 * Supported in provider *v3.11+* and VCD *10.4+* with NSX-T.
 *
 * Provides a data source to read NSX-T Edge Gateway DNS forwarder configuration.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vcd from "@pulumi/vcd";
 *
 * const existing = vcd.getOrgVdc({
 *     name: "existing-vdc",
 * });
 * const testing = existing.then(existing => vcd.getNsxtEdgegateway({
 *     ownerId: existing.id,
 *     name: "server-testing",
 * }));
 * const dns_service = testing.then(testing => vcd.getNsxtEdgegatewayDns({
 *     org: "datacloud",
 *     edgeGatewayId: testing.id,
 * }));
 * ```
 */
export function getNsxtEdgegatewayDnsOutput(args: GetNsxtEdgegatewayDnsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetNsxtEdgegatewayDnsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vcd:index/getNsxtEdgegatewayDns:getNsxtEdgegatewayDns", {
        "edgeGatewayId": args.edgeGatewayId,
        "org": args.org,
    }, opts);
}

/**
 * A collection of arguments for invoking getNsxtEdgegatewayDns.
 */
export interface GetNsxtEdgegatewayDnsOutputArgs {
    /**
     * The ID of the Edge Gateway (NSX-T only). 
     * Can be looked up using [`vcd.NsxtEdgegateway`](https://www.terraform.io/providers/vmware/vcd/latest/docs/data-sources/nsxt_edgegateway) data source
     */
    edgeGatewayId: pulumi.Input<string>;
    /**
     * The name of organization to use, optional if defined at 
     * provider level. Useful when connected as sysadmin working across different organisations
     */
    org?: pulumi.Input<string>;
}
